<%= form_for [@link, @entry], :remote => true do |form| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
        <% @entry.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :word %>
    <%= form.text_field :word %>
  </div>

  <div class="field">
    <%= form.label :reading %>
    <%= form.text_field :reading %>
  </div>

  <div class="field">
    <%= form.label :definition %>
    <%= form.text_field :definition %>
  </div>

  <div class="field">
    <%= form.label :priority %>
    <%= form.check_box :priority %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
